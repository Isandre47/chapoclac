<form method="post" action="{{ path('registration_delete', {'id': registration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registration.id) }}">
    <button class="btn">Delete</button>
</form>
