{% extends '/admin/baseAdmin.html.twig' %}

{% block body %}
    <div class="main-content">
        <div class="section__content section__content--p30">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-12 mb-3">
                        <a class="m-2" href="{{ path('users_index') }}"><i class="fas fa-hand-point-left fa-3x"></i></a>
                        <h2 class="m-3">Modifier l'utilisateur</h2>
                        {{ include('/admin/users/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
