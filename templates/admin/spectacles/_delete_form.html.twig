<form method="post" action="{{ path('spectacles_delete', {'id': spectacle.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce spectacle ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spectacle.id) }}">
    <button class="text-danger fas fa-skull-crossbones fa-lg"></button>
</form>
